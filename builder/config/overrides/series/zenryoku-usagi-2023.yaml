# Authored override — 2025 catalogue import. Holds only the structure the
# open sources cannot express; `builder build` fills the facts into data/.
# Never hand-edit data/.
series:
  id: zenryoku-usagi-2023
  titles:
    original: 全力ウサギ (アニメプロジェクト)
    translations: { ja-Latn: "Zenryoku Usagi (2023)" }
  seasons:
    - id: zenryoku-usagi-2023-s1
      number: 1
      # Authored: upstream does not list this installment under the series'
      # own title, so the build cannot resolve it.
      externalIds: { anilistId: 145000 } # Zenryoku Usagi (2023) (Fall 2023)
    - id: zenryoku-usagi-2023-s2
      number: 2
      externalIds: { anilistId: 184182 } # Zenryoku Usagi (2023) 2nd Season (Spring 2025)
# One linear storyline: episodes carry a continuous absoluteNumber.
numbered: [zenryoku-usagi-2023]
